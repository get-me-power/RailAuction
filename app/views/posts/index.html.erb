<table>
    <tr>
        <th>商品名</th>
    </tr>
    <% @posts.each do |post| %>
        <tr>
            <td><%= link_to(post.product_name, "/posts/#{post.picture}") %></td>
        </tr>
    <% end %>
</table>
