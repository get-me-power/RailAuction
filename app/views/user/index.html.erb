<h1 class="users-heading">ユーザー一覧</h1>
<% @users.each do |user| %>
  <div class="user-right">
    <!--デバック用に画像のサイズは小さめに設定した-->
    <img src="<%= "/user_images/#{user.image_name}" %>" align="left" width="30" height="25">
    <%= link_to(user.name, "/user/#{user.image_name}") %>
  </div>
<% end %>
