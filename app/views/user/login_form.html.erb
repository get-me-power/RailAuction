<% if @error_message %>
  <div class="form-error">
    <%= @error_message %>
  </div>
<% end %>
<% @user = User.new %>
<%= form_for(@user, :url => {:action => :login, :controller => :user}) do |f| %>
    
  <%= f.label :メールアドレス %>
  <%= f.text_field :email ,:value => "#{@email}" %>
  <br>
  <%= f.label :パスワード %>
  <%= f.password_field :password %>
  
  <%= f.submit  "ログイン" %>
<% end %>
