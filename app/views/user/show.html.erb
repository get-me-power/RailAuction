<b>ユーザー名</b>
<br>
<h2><%= @user.name %></h2>
<b>登録メールアドレス<b>
    <br>
    <p><%= @user.email %></p>

    <% if @user.id == @current_user.id %>
      <%= link_to("/user/#{@current_user.id}/edit") do %>
        <div>
          編集
        </div>
      <% end %>
      <%= link_to("/user/#{@current_user.id}/destroy", {method: :post}) do %>
        <div>
          退会
        </div>
      <% end %>
    <% end %>
